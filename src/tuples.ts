/* --------------------------------------------------------------------------
 * APUNTES:
 * 		   Las tuplas me permiten definir los tipos de datos de cada índice
 *         de un <array>.
-------------------------------------------------------------------------- */

const arrays: Array<string> = ['a', 'b', 'c'];
const arrayUnion: Array<string | number> = ['a', 2, 'c', 1];

// Tupla
const tuple: [string, number, boolean, number] = ['a', 1, true, 2];
